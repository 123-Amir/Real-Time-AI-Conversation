# 🎙 Real-Time AI Conversation

Small React app where an AI talks with a child about an image for ~1 minute using voice.

## Objective
- Show an engaging image.
- AI starts and sustains a 1‑minute voice conversation based on that image.
- Use at least one **tool call** to update the UI during/after the chat.

## Features
- React UI with big image and simple controls.
- Speech input (child talks), AI replies with voice.
- 1‑minute session timer and friendly end message.
- Tool call triggers a UI change (e.g. badge, fun fact, highlight).

## Tech
- React (Vite + TypeScript).
- Any LLM API/SDK for responses.
- Browser speech APIs or voice SDK for STT/TTS.

## Run
npm install
npm run dev
